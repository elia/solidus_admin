<% admin_breadcrumb(link_to plural_resource_name(Spree::Product), solidus_admin.products_path) %>
<% admin_breadcrumb(plural_resource_name(Spree::Taxonomy)) %>


<% content_for :page_actions do %>
  <% if can?(:create, Spree::Taxonomy) %>
    <li>
      <%= link_to t('spree.new_taxonomy'), solidus_admin.new_taxonomy_url, id: 'admin_new_taxonomy_link', class: 'btn btn-primary' %>
    </li>
  <% end %>
<% end %>

<% if @taxonomies.any? %>
<div id="list-taxonomies" data-hook>
  <%= render 'list' %>
</div>
<% else %>
  <div class="no-objects-found">
    <%= render 'solidus_admin/shared/no_objects_found',
                 resource: Spree::Taxonomy,
                 new_resource_url: new_object_url %>
  </div>
<% end %>
