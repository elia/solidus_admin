<% admin_layout "full-width" %>

<% breadcrumb(link_to plural_resource_name(Spree::Promotion), solidus_admin.promotions_path) %>
<% admin_breadcrumb(t('spree.new_promotion')) %>

<%= form_for @promotion, url: collection_url do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= render partial: 'solidus_admin/shared/new_resource_links' %>
<% end %>
