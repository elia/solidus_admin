<% admin_breadcrumb(t('spree.settings')) %>
<% breadcrumb(link_to plural_resource_name(Spree::Zone), solidus_admin.zones_path) %>
<% admin_breadcrumb(@zone.name) %>


<% content_for :page_actions do %>
<% end %>

<%= render partial: 'spree/shared/error_messages', locals: { target: @zone } %>

<%= form_for [@zone] do |zone_form| %>
  <%= render partial: 'form', locals: { zone_form: zone_form } %>
  <div class="clear"></div>
  <%= render partial: 'solidus_admin/shared/edit_resource_links' %>
<% end %>
