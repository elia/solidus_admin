<%= render 'solidus_admin/shared/taxes_tabs' %>

<% admin_breadcrumb(t('spree.settings')) %>
<% admin_breadcrumb(t('spree.admin.tab.taxes')) %>
<% admin_breadcrumb(link_to plural_resource_name(Spree::TaxRate), solidus_admin.tax_rates_path) %>
<% admin_breadcrumb(t('spree.new_tax_rate')) %>


<% content_for :page_actions do %>
<% end %>

<%= render partial: 'spree/shared/error_messages', locals: { target: @tax_rate } %>

<%= form_for [@tax_rate] do |f| %>
  <fieldset class="no-border-top">

    <%= render partial: 'form', locals: { f: f } %>

    <div class="clear"></div>

    <%= render partial: 'solidus_admin/shared/new_resource_links' %>

  </fieldset>
<% end %>
