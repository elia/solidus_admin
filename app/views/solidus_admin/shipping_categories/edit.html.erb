<%= render 'solidus_admin/shared/shipping_tabs' %>

<% admin_breadcrumb(t('spree.settings')) %>
<% admin_breadcrumb(t('spree.admin.tab.shipping')) %>
<% breadcrumb(link_to plural_resource_name(Spree::ShippingCategory), solidus_admin.shipping_categories_path) %>
<% admin_breadcrumb(t('spree.editing_shipping_category')) %>


<% content_for :page_actions do %>
<% end %>

<%= render partial: 'spree/shared/error_messages', locals: { target: @shipping_category } %>

<%= form_for [@shipping_category] do |f| %>
  <fieldset class="no-border-top">
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'solidus_admin/shared/edit_resource_links' %>
  </fieldset>
<% end %>
