<%= render partial: 'solidus_admin/shared/order_tabs', locals: { current: 'Payments' }%>

<% admin_breadcrumb(Spree::Payment.model_name.human) %>
<% admin_breadcrumb(plural_resource_name(Spree::LogEntry)) %>

<% content_for :page_actions do %>
  <li><%= link_to t('spree.logs'), solidus_admin.order_payment_log_entries_url(@order, @payment), class: 'btn btn-primary' %></li>
  <li><%= link_to t('spree.back_to_payment'), solidus_admin.order_payment_url(@order, @payment), class: 'btn btn-primary' %></li>
<% end %>

<%= render 'solidus_admin/payments/log_entries', log_entries: @log_entries %>
